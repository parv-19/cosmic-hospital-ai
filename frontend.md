You are a Senior Product Designer + Frontend Architect.

I have a working React + Tailwind SaaS dashboard for an AI Telephony platform (Admin, Doctor, Read-only panels).

The functionality is correct, but the UI looks like a basic college project.

Your task is to redesign ONLY the UI/UX (not backend logic) to make it look like a modern, production-grade SaaS product similar to Stripe, Vercel, or Linear.

---

GOALS:

* Clean, minimal, professional SaaS look
* Strong visual hierarchy
* Consistent spacing system
* Modern typography
* Subtle shadows and depth
* Clear primary vs secondary actions
* Polished tables and cards

---

DESIGN SYSTEM:

1. Color System:

   * Use neutral base (white / gray)
   * Primary accent: one color (teal or blue)
   * Success: green
   * Warning: yellow
   * Error: red
   * Avoid random pastel cards

2. Typography:

   * Headings: bold, large
   * Body: medium weight
   * Use clear spacing between sections
   * Reduce clutter text

3. Layout:

   * Use grid-based layout
   * Increase whitespace
   * Align all cards properly
   * Maintain consistent padding (p-6 or p-8)

4. Cards:

   * Rounded-xl
   * Soft shadow (shadow-sm / shadow-md)
   * Hover effect
   * Remove heavy borders

5. Sidebar:

   * Dark sidebar OR clean minimal light
   * Active item highlight with accent color
   * Icons for each menu item

6. KPI Cards:

   * Make them visually prominent
   * Add icon + large number + label
   * Use subtle gradient or soft background tint

7. Tables:

   * Add row hover
   * Add spacing between rows
   * Use subtle borders
   * Improve typography

8. Buttons:

   * Primary: solid accent color
   * Secondary: outline
   * Add hover + focus states

9. Live Call Monitor:

   * Make it visually distinct (highlight)
   * Add pulse animation for active calls

---

PAGES TO IMPROVE:

* Admin Dashboard
* Doctor Dashboard
* Read-only Analytics
* Sidebar navigation
* KPI cards
* Tables
* Call logs / transcripts UI

---

CONSTRAINTS:

* Do NOT change API calls
* Do NOT break functionality
* Only improve UI components and layout
* Keep Tailwind CSS

---

OUTPUT:

1. Updated React components
2. Tailwind class improvements
3. Layout restructuring
4. Component reuse strategy
5. Suggested color palette
6. Suggested spacing system

---

GOAL:

Transform this from a basic dashboard into a premium SaaS product UI.
